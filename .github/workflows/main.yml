name: CI Workflow


on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
      - name: Checkout repository
        uses: actions/checkout@v4

      
      - name: Test Workflow
        run: echo "Workflow is working for subiksha-main!"

      
      - name: List files
        run: ls -R
